<div class="container-form my-3 p-5">
  <div>
    <h2 class="text-regular mb-3">Add a new cocktail</h2>
  </div>

  <div>
    <%= simple_form_for(@cocktail) do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.button :submit, class: "btn-black mt-3" %>
    <% end %>
  </div>
</div>
